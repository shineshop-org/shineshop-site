'use client'

export function DataBackupRestore() {
  // This component is intentionally left empty as per the requirement
  // to simplify the data management section to only have a "Public" button
  return null
} 